apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: music-dashboard-qa
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: music-app

  source:
    repoURL: https://github.com/madKrypton/argo-cd-demo.git
    targetRevision: main
    path: k8s/overlays/qa

  destination:
    server: https://kubernetes.default.svc
    namespace: music-app-qa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10

  info:
    - name: Environment
      value: QA
    - name: Team
      value: Platform Engineering
    - name: Approval
      value: Required for Production
